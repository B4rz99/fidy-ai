# Durable execution inventory

This is the baseline classification required by
[ADR 0024](../adr/0024-effect-durable-execution.md). It audits production server source at
`1c04557796e2b63ee26d1f1aaa414526032b099d` (the parent of the pattern-documentation commit). Tests,
fixtures, development seed code, release tooling, and the interactive REPL are excluded unless they
prove a production mechanism. Update the affected row during each migration.

## Dispositions

Every item has one disposition:

- **Effect durable execution** — replace execution-only claims, leases, retries, polling, durable
  waits, or cross-transaction serialization with the Effect facility named in the row.
- **Effect process-local coordination** — keep an Effect primitive because losing its state on
  restart and applying it independently per replica cannot violate correctness.
- **Database constraint or atomic statement** — retain PostgreSQL coordination embedded in the
  write that enforces the invariant.
- **Retained transactional lock** — retain a row or transaction advisory lock around one short
  commit-time invariant; no network work occurs while held.
- **Deliberate existing scheduling** — keep an immediate-at-startup, repeat-safe maintenance loop
  when missing one tick only delays cleanup; issue #469 owns the final consolidation.

“Replace” means expand–migrate–contract under the linked ticket, not immediate deletion by this
audit.

## Durable claims, leases, retry state, and execution statuses

| Owner                                | Current mechanism and protected invariant                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Disposition and contraction                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Onboarding / EmailAuthentication     | `email_delivery_intents` stores `pending/claimed/armed/sent/rejected/uncertain/superseded`, `claim_token`, `claim_expires_at`, generation and provider evidence; `OnboardingDeliveryWorkerLive` reconciles expired armed claims and polls every second (`0034-verified-email-onboarding.ts`, `onboarding/delivery-worker.ts`). It prevents two workers from sending one proof generation and fences stale settlement.                                                                                  | **Effect durable execution — Workflow/Activity, #460.** Retain generation, provider idempotency, and truthful sent/rejected/uncertain evidence where they remain domain/security facts. Delete claim token/expiry, claimable index, reconciliation claim path, worker poll, and execution-only pending/claimed/armed states after cutover.                                                                                                                                                                  |
| Statement Ingestion                  | `statement_submissions.status`, `claim_id`, `attempt_count`, `started_at`, stale-after-15-minutes selection, `FOR UPDATE SKIP LOCKED`, one-second poll, and claim ownership before finalization (`0022-statement-ingestion.ts:9-39,143-210`, `ingestion/repo.ts:250-280`, `ingestion/worker.ts:276-299`). It protects one submission from concurrent processing and bounds mapping retries.                                                                                                            | **Effect durable execution — PersistedQueue, #461 (implemented by migration 0047).** Transactional identifier-only publication now converges on one queue item. Queue leases and attempts own execution; typed mapping exhaustion creates NeedsReviewItems. The migration deletes `claim_id`, attempt/lease recovery, processing eligibility, claim gateway, and the one-second poll while retaining queued/completed/failed domain outcomes, accounting, idempotency, retention failure, and review state. |
| Forwarded Email Ingestion            | `forwarded_email_receipts` stores `queued/deferred/processing/completed/revoked`, `resume_at`, `claim_id`, `attempt_count`, `started_at`; gateway candidates use `FOR UPDATE SKIP LOCKED`; stale work is retried/exhausted; the worker polls every second (`0043-email-ingestion.ts:16-43,486-575`, `ingestion/email-forwarding-repo.ts:414-528`, `ingestion/forwarded-email-ingestion.ts:179-188`). It protects one provider retrieval/interpretation and implements Consent deferral and exhaustion. | **Effect durable execution — Workflow/Activities plus DurableClock, #462.** Retain accepted receipt identity, User/context, quota accounting, Consent/domain outcomes, Transaction/NeedsReviewItem links, and provider evidence. Delete claim id, attempt count, stale processing recovery, due-row selection, execution statuses and poller once the workflow owns continuation.                                                                                                                           |
| Browser-pairing email authentication | `browser_pairing_email_start_requests` and `browser_pairing_email_delivery_intents` use pending/claimed/armed states, claim tokens, two-minute leases, `SKIP LOCKED`, stale armed reconciliation, bounded sender retries, and one-second polling (`0039-verified-email-login.ts:42-78,176-279`, `authentication-delivery-worker.ts`).                                                                                                                                                                  | **Effect durable execution — Workflow/Activities and durable expiry, #463.** Retain the User-owned authentication workflow, proof digest/generation, provider evidence, and terminal security meaning. Delete request/delivery claims, leases, claim gateways, stale reconciliation and delivery poll. Start-request continuation becomes part of the stable workflow rather than a second queue.                                                                                                           |
| Verified-email replacement           | `email_replacement_delivery_intents` and `email_replacement_workflows.retention_claim_*` use delivery and cleanup claims, leases, `SKIP LOCKED`, stale-armed reconciliation, and one-second/minute polls (`0036-verified-email-replacement.ts:7-49,96-139`, `replacement-delivery-worker.ts`, `replacement-retention.ts`).                                                                                                                                                                             | **Effect durable execution — Workflow/Activities and durable expiry, #464.** Retain the replacement lifecycle, proof/generation, credential revision, provider evidence, and atomic final replacement. Delete delivery and retention claim columns/functions, stale reconciliation, and both polls after workflow cutover.                                                                                                                                                                                  |
| Hosted Turns                         | A session advisory lock on `hosted-attempt:<UserId>` reserves one SQL connection across admission, model/provider work, delivery, and terminalization (`db/advisory-lock.ts:11-15,60-101`, `agent/agent-service.ts:1603`). `conversation_turns.state` records Pending/Completed/Failed/Interrupted domain meaning.                                                                                                                                                                                     | **Effect durable execution — keyed Cluster entity, #465.** Delete the session lock and reserved connection. Retain Turn and Hosted Agent Session lifecycle, recovery, Transcript, and Compaction state: those explain domain continuity rather than implementing a lease.                                                                                                                                                                                                                                   |
| WhatsApp Consent disclosure          | `whatsapp_consent_disclosure_delivery_attempts` combines verified attempt evidence with `claimed/started/reconciliation-required/retry-scheduled/...`, `claim_expires_at`, `retry_at`, attempt number, due-row `SKIP LOCKED`, and the WhatsApp worker poll (`0017-recover-consent-disclosure-delivery.ts`, `channels/whatsapp/disclosure-store.ts`, `channels/whatsapp/worker.ts`).                                                                                                                    | **Effect durable execution — Workflow/Activities and DurableClock, #466.** Retain attempt identity, provider/correlation evidence, certainty, safe reason, delivered/reconciliation/terminal outcome needed by ADR 0013. Delete claim leases, due-retry rows/selection, execution-only retry scheduling, and shared poll after workflow evidence proves the same transitions.                                                                                                                               |
| WhatsApp inbound receipt             | `whatsapp_inbound_receipts` claims each provider message for 30 seconds and stores processing/outbound_started/completed to prevent unsafe webhook acknowledgement and replay (`0012-create-whatsapp-channel.ts:75-86,251-367`, `channels/whatsapp/repo.ts:190-267`).                                                                                                                                                                                                                                  | **Effect durable execution — transactional durable publication plus workflow/queue, #467.** Retain authenticated provider replay evidence and the irreversible outbound-start distinction if still required for truthful redelivery. Delete claim id/expiry and processing lease once duplicate publication converges on the same execution.                                                                                                                                                                |
| WhatsApp inbound Turn                | `whatsapp_turn_claims`, `whatsapp_inbound_jobs.claim_id`, `processing_attempt`, debounce timestamps, per-User advisory admission, `SKIP LOCKED`, lease-expiry recovery, eight 250-ms pollers, and terminal claim rows implement burst collapse and per-User serialization (`0012-create-whatsapp-channel.ts:17-47,385-472`, `0018-whatsapp-durable-propagation.ts`, `channels/whatsapp/repo.ts:464-710`, `channels/whatsapp/worker.ts:169-207`).                                                       | **Effect durable execution — DurableQueue plus keyed Cluster execution, #467 (after #465/#466).** Keep message evidence, bounded content until processing, occurrence/order, debounce policy, completed delivery evidence, and trace projection. Delete Turn claim table, claim/lease fields, processing attempt, shared advisory admission key, stale-claim actions, and worker polls.                                                                                                                     |
| Subscription                         | CardEnrollment's `prepared/creating/available/refused/expired/verifying` states and provider idempotency key fence Wompi source creation. `withEnrollmentConsent` and `prepareCardEnrollment` currently hold Consent/subscription session locks and SQL transactions across Wompi contract retrieval (`subscription/enrollment-handlers.ts`, `subscription/card-enrollment.ts`, `0042-wompi-card-enrollments.ts`). No BillingAttempt implementation exists at this baseline.                           | **Effect durable execution — Workflow/Activities, #468**, for Wompi continuation and future BillingAttempt work. Retain CardEnrollment lifecycle and the short `beginSubmission`/settlement transaction locks. Delete the network-spanning Consent and subscription lock/transaction scopes, moving provider calls to Activities outside transactions.                                                                                                                                                      |
| Short in-attempt retries             | Email delivery performs bounded exponential sleeps (`email-authentication/delivery-retry.ts`); WhatsApp reply delivery retries only definitive retryable rejection (`channels/whatsapp/reply-delivery.ts`); hosted inference has bounded within-Turn retry delay (`agent/agent-service.ts:806`).                                                                                                                                                                                                       | **Effect process-local coordination** until each owning workflow migration. They are safe only where restarting the owning idempotent Activity is safe. #460/#463/#464/#466/#467 decide whether Activity policy replaces them; no persisted retry framework may be added.                                                                                                                                                                                                                                   |

Wrong-proof counts, PAT polling slowdown, authentication lifecycle, NeedsReviewItem status, Turn state,
CardEnrollment state, provider attempt evidence, and callback reconciliation are not automatically
“execution state” because they encode security or User-visible meaning. Migration tickets must
separate that meaning from lease mechanics rather than deleting whole tables by name.

## Advisory locks

### Registry locks

| Lock key / callers                                                                                  | Immediate invariant                                                                                                                      | Disposition                                                                                                                                                                                                                              |
| --------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `keywordRules` (`categories/repo.ts`)                                                               | Serialize priority/order changes for one User inside the mutation transaction.                                                           | **Retained transactional lock.**                                                                                                                                                                                                         |
| `budgets` (`budgets/mutations.ts`)                                                                  | Serialize one User's bounded Budget set and associated immediate mutation decision.                                                      | **Retained transactional lock.**                                                                                                                                                                                                         |
| `browserLoginApproval` (`browser-login/repo.ts`, browser-pairing delivery/authentication, Recovery) | Serialize proof approval, recovery approval, delivery settlement, and WebSession creation for one User.                                  | **Retained transactional lock.** It is a short security transition lock; #463 may remove delivery-only acquisitions only if the same transactional invariant remains protected.                                                          |
| `backupRecoveryRotation` (`recovery/service.ts`)                                                    | Non-blockingly ensure one BackupRecoveryCode rotation wins under current authority.                                                      | **Retained transactional lock.**                                                                                                                                                                                                         |
| `subscriptionEnrollment` (`subscription/card-enrollment.ts`)                                        | Serialize CardEnrollment prepare/submit/settle state for one User; preparation currently includes Wompi contract retrieval.              | Commit-only acquisitions: **retained transactional lock**. Network-spanning preparation acquisition: **Effect durable execution, #468**; delete that scope while retaining the commit-time transition lock.                              |
| `emailReplacementCandidate` (`replacement-transition.ts`)                                           | Serialize globally unique candidate-mailbox admission with the User transition.                                                          | **Retained transactional lock.**                                                                                                                                                                                                         |
| `dashboard` (`dashboard/repo.ts`)                                                                   | Serialize one User's DashboardDocument revision and mutation.                                                                            | **Retained transactional lock.**                                                                                                                                                                                                         |
| `memories` (`memory/mutations.ts`)                                                                  | Serialize Memory revision and bounded collection changes.                                                                                | **Retained transactional lock.**                                                                                                                                                                                                         |
| `consentSubject` (`consent/repo.ts`)                                                                | Order current Consent checks/evidence with consent-dependent work and revocation; Subscription currently wraps Wompi calls in this lock. | Short database acquisitions: **retained transactional lock** under ADR 0008. Subscription's network-spanning acquisition: **Effect durable execution, #468**; replace it with short checks/settlement that preserve revocation ordering. |
| `consentGate` (`consent/repo.ts`)                                                                   | Serialize pre-User WhatsApp caller onboarding exchange creation/decision.                                                                | **Retained transactional lock.** Provider identity is the protected resource before a User exists.                                                                                                                                       |
| `hostedAttempt` / `withUserTurnLock`                                                                | Serialize a User's hosted execution across transactions and network calls.                                                               | **Effect durable execution — Cluster entity, #465.** Delete.                                                                                                                                                                             |
| `consentExternalEffect` / `withConsentExternalEffectLock`                                           | Hold a reserved session across forwarded-email provider/model egress so revocation cannot overtake work.                                 | **Effect durable execution — workflow/entity coordination, #462.** Delete the cross-transaction session lock while preserving ADR 0008's ordering.                                                                                       |
| `whatsAppAdmission` shared bare-User key                                                            | Serialize enqueue debounce extension with global Turn claiming across transactions.                                                      | **Effect durable execution — DurableQueue/keyed entity, #467.** Delete after one execution substrate owns per-User ordering.                                                                                                             |

### SQL-local advisory locks

| Location                                                                                            | Immediate invariant                                                                                                                  | Disposition                                                                                                                                                                       |
| --------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `browser-login/repo.ts:105` and `browser-pairing-authentication.ts:103`                             | Cheap global start capacity and source-address admission before inserting bounded anonymous work.                                    | **Database constraint or atomic statement** pending the shared-store evaluation in #470. These are distributed security admission, not durable execution.                         |
| `0035-pat-pairings.ts:155,204,243` and `pat-pairing-persistence.ts:418`                             | Serialize global PATPairing start capacity, anonymous claim-source counters, and per-User inspection counters.                       | **Database constraint or atomic statement** pending #470. Preserve source/User anti-bypass and atomic Retry-After evidence.                                                       |
| `0040-support-recovery.ts:262` and `recovery/repo.ts:88`                                            | Serialize global open-case capacity and anonymous recovery-attempt accounting.                                                       | **Database constraint or atomic statement** pending #470.                                                                                                                         |
| `0043-email-ingestion.ts:63,125,226,270`                                                            | Atomically enforce global outstanding capacity, authenticated Resend replay/rate admission, and known-recipient global/User windows. | **Database constraint or atomic statement.** These compose capacity/replay/admission with the accepted receipt; #470 may replace only a counter whose atomicity and keys survive. |
| `0012-create-whatsapp-channel.ts:197`                                                               | Make one provider message consume one ingress budget at most once.                                                                   | **Database constraint or atomic statement.** It is replay-coupled security admission, not worker ownership.                                                                       |
| `support-recovery:open-capacity`, forwarded-email numeric locks, and the source-counter locks above | Serialize count-then-insert statements where no single row exists before first admission.                                            | **Database constraint or atomic statement.** Prefer uniqueness/upsert where a migration can make the same invariant structural, but do not replace them with process-local state. |

## Row locks and `SKIP LOCKED`

The table lists every production lock-bearing module; migration files are named where the lock lives
inside a stored function.

| Owner / locations                                                                                                                                                     | Protected invariant                                                                                                                                     | Disposition                                                                                                                                                                                                                                                |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| BrowserLogin (`browser-login/repo.ts:183,232`; `0027-web-sessions.ts:81`)                                                                                             | Consume/approve one pairing and WebSession transition atomically.                                                                                       | **Retained transactional lock.**                                                                                                                                                                                                                           |
| Identity/WhatsApp resolution (`0015-whatsapp-bsuid-identity.ts:168,209`; `identity/repo.ts`)                                                                          | Bind or update one authoritative WhatsAppIdentity without reassociation races.                                                                          | **Retained transactional lock.**                                                                                                                                                                                                                           |
| Consent (`consent/repo.ts:1075`)                                                                                                                                      | Select a bounded expired pre-User batch before deletion.                                                                                                | **Deliberate existing scheduling** with bounded maintenance `SKIP LOCKED`; #469 confirms.                                                                                                                                                                  |
| WhatsApp disclosure (`0017-recover-consent-disclosure-delivery.ts:77,116,315`)                                                                                        | Lock pending exchange/provider evidence; one use is due-retry worker selection.                                                                         | Exchange/evidence transition: **retained transactional lock**. Due-retry `SKIP LOCKED`: **Effect durable execution, #466**.                                                                                                                                |
| Statement Ingestion (`0022-statement-ingestion.ts:187`; `ingestion/repo.ts:143,275,604`)                                                                              | Former claim selection plus the current Free backfill entitlement, terminal finalization, and review resolution.                                        | Claim selection/fence: **Effect durable execution, #461 (implemented)**; PersistedQueue owns selection while one short terminal row lock remains idempotent. Entitlement and review transitions: **retained transactional lock**.                          |
| PAT lifecycle (`0033-fixed-pat-lifetimes.ts:106`; `0035-pat-pairings.ts:290,304,344,395,451`; `tokens/repo.ts:128,149,172`)                                           | Single-use proof/claim, review binding, claim-vs-revoke lock order, and expiry transition.                                                              | **Retained transactional lock** for proof and lifecycle transitions. Expired approved-pairing `SKIP LOCKED` is **deliberate existing scheduling** pending #469; it is not worker execution.                                                                |
| Verified-email replacement (`0036-verified-email-replacement.ts:115,133,147`; `replacement-transition.ts:88,103`)                                                     | Delivery/retention worker claims plus current workflow/WebSession transition.                                                                           | Delivery/retention `SKIP LOCKED`: **Effect durable execution, #464**. Workflow/WebSession transition: **retained transactional lock**.                                                                                                                     |
| Browser-pairing email (`0039-verified-email-login.ts:190,242,263,275`; `browser-pairing-authentication.ts:374,593,694`)                                               | Start/delivery/retention claims plus current request/workflow transition.                                                                               | Claim `SKIP LOCKED`: **Effect durable execution, #463**. Request/workflow transition locks: **retained transactional lock** until the workflow migration proves equivalent atomic settlement.                                                              |
| Email onboarding (`email-authentication/repo.ts:41,192,210,338,434,456`; `email-authentication/admission.ts:106`)                                                     | Allocate one of four transaction slots, consume proof/enrollment, claim delivery, bounded retention, and atomically charge requester+recipient budgets. | Delivery claim: **Effect durable execution, #460**. Enrollment/proof and two-budget locks: **retained transactional lock**. Slot allocation: **database constraint or atomic statement**. Bounded retention selection: **deliberate existing scheduling**. |
| SupportRecovery (`0040-support-recovery.ts:144,274,300`; `recovery/repo.ts:237,265`)                                                                                  | Append ordered case evidence, consume/rotate one recovery credential, expire cases, and delete terminal evidence in bounded batches.                    | Lifecycle/credential: **retained transactional lock**. Bounded expiry/retention `SKIP LOCKED`: **deliberate existing scheduling**, #469.                                                                                                                   |
| Forwarded Email (`0043-email-ingestion.ts:70,144,234,237,292,517`; `email-forwarding-repo.ts:181,526`)                                                                | Enforce atomic global/User/deferred capacity, rate windows, claim work, and fence finalization.                                                         | Work claim/fence: **Effect durable execution, #462**. Capacity/rate/address rows: **database constraint or atomic statement**, with counters evaluated by #470.                                                                                            |
| Transcript/Hosted Agent Session (`conversation-continuity.ts:331,481`; `hosted-agent-session.ts:74,133`)                                                              | Serialize revision, terminalize only Pending Turn, and update one active Hosted Agent Session.                                                          | **Retained transactional lock.** #465 removes only the cross-transaction session advisory lock; these short domain transitions remain.                                                                                                                     |
| Categories, Dashboard, Budgets, Memories, Insights (`categories/repo.ts`, `dashboard/repo.ts`, `budgets/mutations.ts`, `memory/mutations.ts`, `insights/repo.ts:180`) | Serialize one aggregate's immediate mutation/revision/delivery transition.                                                                              | **Retained transactional lock.**                                                                                                                                                                                                                           |
| Email anonymization and NeedsReviewItem (`email-anonymization-approval.ts:49`, `ingestion/repo.ts:604`)                                                               | Atomically approve one sample or resolve one review.                                                                                                    | **Retained transactional lock.**                                                                                                                                                                                                                           |
| Historical PAT gateway (`0028-persisted-schema-reconciliation.ts:979`)                                                                                                | Superseded bearer-use function definition; migration 0033 replaces this lock in the live schema.                                                        | **Database constraint or atomic statement.** Historical definition only; the live lock is classified in the PAT lifecycle row above.                                                                                                                       |

## Polling, expiry, and retention loops

| Production loop                                                              | Timing meaning                                                                                                                            | Disposition                                                                                                                                        |
| ---------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------- |
| Onboarding delivery (`onboarding/delivery-worker.ts`)                        | One-second due-row polling for provider Work.                                                                                             | **Effect durable execution, #460.** Delete.                                                                                                        |
| Statement Ingestion (`ingestion/worker.ts`)                                  | Former one-second claim polling.                                                                                                          | **Effect durable execution, #461 (implemented).** Native SQL PersistedQueue polling replaces the application poll; no Fidy claim schedule remains. |
| Forwarded Email processor (`ingestion/forwarded-email-ingestion.ts:179-188`) | One-second claim polling.                                                                                                                 | **Effect durable execution, #462.** Delete.                                                                                                        |
| Browser-pairing email delivery (`authentication-delivery-worker.ts`)         | One-second start-request and delivery polling.                                                                                            | **Effect durable execution, #463.** Delete.                                                                                                        |
| Email replacement delivery (`replacement-delivery-worker.ts`)                | One-second delivery polling.                                                                                                              | **Effect durable execution, #464.** Delete.                                                                                                        |
| WhatsApp workers (`channels/whatsapp/worker.ts`)                             | Eight 250-ms loops combine due disclosure retries and inbound Turn claims.                                                                | **Effect durable execution, #466/#467.** Delete the combined polling substrate.                                                                    |
| Browser-pairing email workflow purge (`authentication-retention.ts`)         | One-second retention-claim poll for User-owned workflow expiry.                                                                           | **Effect durable execution — durable expiry, #463.** Delete.                                                                                       |
| Replacement workflow purge (`replacement-retention.ts`)                      | One-minute retention-claim poll.                                                                                                          | **Effect durable execution — durable expiry, #464.** Delete claim loop.                                                                            |
| Browser login anonymous evidence (`browser-login/handlers.ts`)               | Repeat-safe deletion every ten minutes; missed ticks only delay bounded cleanup and startup runs before the first delay call is repeated. | **Deliberate existing scheduling, #469.**                                                                                                          |
| Onboarding pre-User retention (`onboarding/retention.ts`)                    | Immediate and hourly bounded cleanup.                                                                                                     | **Deliberate existing scheduling, #469.** Expiration is enforced on use; cleanup may lag.                                                          |
| PATPairing expiry (`tokens/pairing-expiry.ts`)                               | Every minute expires/revokes due short-lived pairings through atomic lifecycle functions.                                                 | **Deliberate existing scheduling, #469** unless durable timing is proved necessary. Lifecycle checks already reject expired use.                   |
| SupportRecovery retention (`recovery/retention.ts`)                          | Immediate and minutely bounded lifecycle expiry and 24-month cleanup.                                                                     | **Deliberate existing scheduling, #469.** Authoritative operations check expiry independently.                                                     |
| Evidence retention (`evidence-retention.ts`)                                 | Immediate and daily AuditLogEntry/email-lifecycle legal cleanup.                                                                          | **Deliberate existing scheduling, #469.** Missed ticks delay deletion but do not authorize work.                                                   |
| Forwarded email sample retention (`forwarded-email-ingestion.ts:192-200`)    | Immediate and daily raw IngestSample expiry.                                                                                              | **Deliberate existing scheduling, #469.** Keep the fixed retention cutoff and alert on lag.                                                        |
| WhatsApp operational retention (`channels/whatsapp/worker.ts:177-207`)       | Hourly cleanup of expired windows and admission receipts.                                                                                 | **Deliberate existing scheduling, #469.** Separate it from the worker poll.                                                                        |
| Browser-pairing email admission cleanup (`authentication-retention.ts`)      | Immediate/minutely bounded anonymous counter cleanup.                                                                                     | **Deliberate existing scheduling**, subject to #470's counter decision.                                                                            |
| Uncomposed `AuditRetentionLive` (`audit/retention.ts`)                       | Duplicate standalone daily maintenance adapter; production composes `EvidenceRetentionLive` instead.                                      | **Deliberate existing scheduling, #469.** Delete the duplicate adapter; retain one immediate, repeat-safe AuditLogEntry cleanup composition.       |

Request equalization sleeps, provider retry delays, and idle sleeps are not schedulers. They remain
scoped to their request/attempt and may be interrupted without losing a separately accepted durable
continuation.

## Admission windows and capacity

| Control                                                                                                                     | Scope and invariant                                                                                                      | Disposition                                                                                                                                             |
| --------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| BrowserLogin start/redeem Semaphores (`browser-login/handlers.ts`)                                                          | Per-process sockets/CPU and cheap overload refusal.                                                                      | **Effect process-local coordination.** Keep; distributed security admission remains separate.                                                           |
| Email authentication start/completion Semaphores (`authentication-handlers.ts`)                                             | Per-process proof-work concurrency.                                                                                      | **Effect process-local coordination.** Keep.                                                                                                            |
| PATPairing start/claim Semaphores (`tokens/pairing-handlers.ts`)                                                            | Per-process anonymous request concurrency.                                                                               | **Effect process-local coordination.** Keep.                                                                                                            |
| Resend webhook Semaphore (`ingestion/resend-webhook.ts`)                                                                    | Per-process exact-body/signature resource lane.                                                                          | **Effect process-local coordination.** Keep.                                                                                                            |
| WhatsApp webhook body-reader Semaphore (`channels/whatsapp/routes.ts`)                                                      | At most 32 body readers per process.                                                                                     | **Effect process-local coordination.** Keep.                                                                                                            |
| `browser_login_start_attempts` and global live-pairing capacity (`0026-browser-login-pairings.ts`, `browser-login/repo.ts`) | Distributed anonymous-source start rate and bounded live state.                                                          | **Database atomic statement**, evaluate shared `RateLimiter` in #470. Live-row capacity stays transaction-coupled.                                      |
| `email_delivery_admission_budgets`                                                                                          | Row locks atomically charge requester plus recipient 24-hour allowance in the same transition that creates a generation. | **Retained transactional lock.** #470 may replace only if one shared transaction can still charge both without weakening recipient anti-bypass.         |
| `email_verification_admission_slots`                                                                                        | Four concurrent completion transactions across replicas.                                                                 | **Database atomic statement.** This is transaction capacity, not a time-window rate limiter.                                                            |
| `email_pairing_login_admission_scopes/attempts`                                                                             | Distributed pairing, anonymous source, and mailbox proof-attempt windows.                                                | **Database atomic statement**, candidate for #470 only with the same composite keys, privacy, Retry-After, and restart behavior.                        |
| `pat_pairing_start_attempts`, `pat_pairing_claim_attempts`, `pat_pairing_inspection_attempts`                               | Anonymous-source burst/window limits, per-User review limits, polling slowdown, and live-state cap.                      | **Database atomic statement**, evaluate #470. Pairing lifecycle and one-time proof state remain PostgreSQL-owned even if counters move.                 |
| `support_recovery_admission_attempts` plus open-case cap                                                                    | Anonymous source/digest anti-enumeration and at most 100 open cases.                                                     | Attempts: **database atomic statement**, evaluate #470. Open-case capacity: **database constraint or atomic statement**, not a standalone rate limiter. |
| `resend_webhook_admission_window` plus delivery replay ledger                                                               | Authenticated-provider global resolution rate, replay, and bounded 10-minute evidence.                                   | **Database atomic statement.** Replay and admission are transaction-coupled; #470 must not split them.                                                  |
| `forwarded_email_known_admission_window` and `forwarded_email_user_admission_windows`                                       | Global/minute and stable-User/hour admission in the accepted receipt transition.                                         | **Database atomic statement**, evaluate #470 but preserve stable-User anti-bypass and transactional publication.                                        |
| Forwarded-email outstanding/deferred capacity                                                                               | 200 global, 100 per User outstanding, 50 per User deferred, enforced by trigger and locked capacity rows.                | **Database constraint or atomic statement.** It bounds durable retained Work, not request rate.                                                         |
| `whatsapp_ingress_budgets` and receipts                                                                                     | Distributed caller/User ingress rate charged once per provider message in the authenticated enqueue transaction.         | **Database atomic statement.** Transaction/replay coupled; #470 may not replace it with memory state.                                                   |
| Browser-login approval rejection count (`browser-login/repo.ts`)                                                            | User-stable rejection evidence read while holding the approval transition's transaction lock.                            | **Retained transactional lock.** Evaluate #470 only if existing Audit atomicity and Retry-After survive.                                                |
| Manual PAT issuance count (`tokens/pat-pairing.ts`)                                                                         | User-stable spend evidence read while holding the issuance transition's transaction lock.                                | **Retained transactional lock.** Evaluate #470 only if existing Consent/Audit atomicity and Retry-After survive.                                        |

Memory-backed Effect `RateLimiter` is rejected for every distributed row above. #470 may adopt
Effect `RateLimiter` only after proving a shared store across two runtimes and net deletion; Redis is
a separate infrastructure decision.

## Migration ownership and deletion ledger

| Ticket | Owning slice                     | Bespoke infrastructure expected to disappear                                                                                                                                                                                                                                              |
| ------ | -------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| #460   | Onboarding / EmailAuthentication | Onboarding delivery claim token/expiry, claim and stale-armed reconciliation functions, one-second worker, execution-only delivery states/indexes.                                                                                                                                        |
| #461   | Ingestion                        | **Deleted in migration 0047:** statement claim id, attempt/lease recovery, `fidy_claim_statement_submission`, worker `SKIP LOCKED`, one-second poll, and execution-only processing state.                                                                                                 |
| #462   | Ingestion                        | Forwarded-email claim id/attempt/stale recovery, due/deferred polling, claim candidate/functions, one-second poll, Consent session advisory lock.                                                                                                                                         |
| #463   | EmailAuthentication              | Browser email start/delivery/retention claims and leases, stale reconciliation, claim functions, delivery and one-second retention polls.                                                                                                                                                 |
| #464   | EmailAuthentication              | Replacement delivery and retention claims/leases/functions, delivery and retention polls.                                                                                                                                                                                                 |
| #465   | Agent / Transcript               | Hosted-attempt session advisory lock and reserved SQL connection.                                                                                                                                                                                                                         |
| #466   | WhatsApp / Consent               | Disclosure claim lease, due retry scheduling/selection, worker polling portions; retain verified provider evidence.                                                                                                                                                                       |
| #467   | WhatsApp                         | Turn claim table, `whatsapp_inbound_jobs` claim/attempt fields, receipt execution lease where replay evidence supersedes it, shared admission lock, eight poll loops.                                                                                                                     |
| #468   | Subscription                     | Delete `withEnrollmentConsent` around whole requests and the Wompi-contract call inside `prepareCardEnrollment`'s SQL transaction/session lock; retain narrow Consent checks, CardEnrollment lifecycle, and commit-time transition locks. No BillingAttempt machinery exists at baseline. |
| #469   | Cross-cutting shell              | Duplicate or superseded retention/expiry loops and retention claims; deliberate immediate maintenance stays explicit.                                                                                                                                                                     |
| #470   | Security admission owners        | Only counters whose shared-store proof permits deleting the PostgreSQL table, lock, cleanup, and counter code.                                                                                                                                                                            |
| #471   | Repository                       | Shared obsolete lock keys/helpers, queue/lease vocabulary and SQL functions, dual-system remnants; add a narrow guard for future bespoke execution machinery.                                                                                                                             |

The final contraction gate is not satisfied by wrapping the old mechanism, retaining dual claims as a
fallback, or leaving a dormant old poller. Every surviving lock and status must map to a row above or
be added with its immediate invariant and owner.
