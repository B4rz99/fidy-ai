import { Outlet } from "@tanstack/react-router";

/** Hosts independently owned application route subtrees without making product decisions. */
export const ApplicationRoot = (): React.JSX.Element => <Outlet />;
